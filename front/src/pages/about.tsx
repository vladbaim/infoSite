import * as React from 'react';
import type { NextPage } from 'next';
import { Box } from '@mui/material';

const About: NextPage = () => {
  return <Box sx={{ height: 300 }}>About</Box>;
};

export default About;
